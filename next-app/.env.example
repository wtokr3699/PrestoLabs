# Firebase Client
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin (서비스 계정 키 - 서버 전용)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Toss Payments Sandbox
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_...
TOSS_SECRET_KEY=test_sk_...

# LLM API
LLM_API_KEY=
LLM_PROVIDER=anthropic

# 크론 작업 인증 (마감일 처리). 미설정 시 /api/cron/* 비활성화
CRON_SECRET=

# 시드 엔드포인트 인증. 미설정 시 /api/seed/* 비활성화 (요청 헤더 x-seed-secret 로 전달)
SEED_SECRET=

# 샌드박스 결제 허용 플래그. "true" 일 때만 sandbox_pk_ 결제 우회 허용.
# 서버가 VERCEL_ENV(Vercel) 또는 NODE_ENV(로컬)로 운영 여부를 판단해
# 운영(Production) 환경에서는 이 값과 무관하게 항상 차단됨.
# Vercel에 추가할 경우 반드시 "Preview" 환경에만 체크하고 "Production"은 비워둘 것.
ALLOW_SANDBOX_PAYMENTS=

# 위 플래그와 함께 켜는 클라이언트용 스위치 (목업 결제 모달 UI를 보여줄지 여부).
# 실제 보안 차단은 서버의 ALLOW_SANDBOX_PAYMENTS가 담당하므로 이 값은 UI 표시 전용.
NEXT_PUBLIC_ALLOW_SANDBOX_PAYMENTS=
